import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "FAQ | Paradigma",
};

export default function FAQPage() {
  return (
    <div>
      <h1>FAQ Page</h1>
    </div>
  );
}
